#连接config-server服务
spring:
  cloud:
    config:
      name: application-8001    #要读取配置文件的名字
      profile: test                 #要加载的环境
      label: master                 #git上的哪个分支
      uri: http://localhost:7500  #连接到config-server的路径